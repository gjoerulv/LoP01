# Review diff command context

Review diffs against the active v3 roadmap.

Use `docs/implementation_roadmap.md` as the only source for current milestone state. Use `docs/content_scope_v3.md` as the active scope cap. Use `CLAUDE.md` for durable workflow/system-boundary guidance.

Reject changes that silently broaden into unselected v3 candidates: full character creation, full settings/mods/accessibility, save-slot metadata/browsers, full Scenario Region Context, fog/reveal, threat preview, AI economy, item economy, battle-depth rewrites, or final service-management UI.

Check that changes preserve completed v1/v2 systems and any completed v3 milestone semantics unless explicitly scoped by the roadmap. Flag stale docs or agent guidance that contradicts `docs/implementation_roadmap.md`.
