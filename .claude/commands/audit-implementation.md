# Audit implementation command context

Audit implementation against the exact commit/branch provided by the user. Do not rely on cached branch state.

Read `docs/implementation_roadmap.md` for current baseline and milestone status. Read `docs/content_scope_v3.md` for the active scope cap. Read `CLAUDE.md` for durable agent/workflow guidance.

Reject scope creep into unselected v3 candidates unless the user explicitly changed the roadmap. Always check docs/source alignment, save/load compatibility, content/runtime separation, gameplay-rule ownership, performance traps, and stale agent guidance.
